// Original judge statements are linked; explanations below are lecture notes.
export const stringProblems = {
  aho: [
    {
      judge: 'AtCoder', id: 'ABC362 G', title: 'Count Substring Query',
      url: 'https://atcoder.jp/contests/abc362/tasks/abc362_g',
      stage: '시작', topic: 'Failure Tree 누적', anchor: 'count', concept: '상태 방문 횟수 → 패턴 등장 횟수',
      task: '본문 S와 Q개의 패턴이 주어진다. 각 패턴이 S에 등장하는 횟수를 구한다. 겹치는 등장도 각각 센다.',
      approach: '질의 패턴을 모두 Trie에 넣고 본문을 한 번 읽는다. 각 문자 처리 후 도착한 상태의 cnt를 1 증가시킨다. 이후 BFS 역순으로 자식의 cnt를 실패 부모에 더한다.',
      formula: String.raw`\mathrm{cnt}[\mathrm{fail}[v]]\mathrel{+}=\mathrm{cnt}[v]\quad(v\ne0),\qquad \mathrm{ans}_i=\mathrm{cnt}[\mathrm{end}_i]`,
      reason: '패턴이 현재 위치에서 끝난다는 것은 그 패턴의 끝 노드가 현재 상태의 실패 조상이라는 뜻이다. 방문 횟수를 조상으로 올리면 각각의 등장 위치가 해당 패턴에 정확히 한 번 전달된다.',
      hint: '출력 링크를 매번 순회할 필요가 없다. 같은 문자열을 묻는 질의는 같은 끝 노드를 공유하며 답도 같다. 루트의 값을 루트에 다시 더하지 않는다.',
      complexity: String.raw`O(M+26V+N+Q)`, costNote: 'Q는 질의 수. 모든 매칭 위치를 나열하지 않으므로 실제 등장 횟수의 합이 시간복잡도에 들어가지 않는다.',
      compare: {url:'/tutorial/suffix-array-lcp/#exercise-atcoder-abc362-g',label:'같은 문제를 Suffix Array의 구간 검색으로 풀기 →'},
    },
    {
      judge:'Codeforces', id:'1202E', title:'You Are Given Some Strings...',
      url:'https://codeforces.com/problemset/problem/1202/E',
      stage:'확장',topic:'양방향 자동자',anchor:'output',concept:'실패 링크를 통한 terminal 개수 전파',
      task:'본문 t에서 두 패턴을 이어 붙인 문자열이 등장하는 횟수를 모든 순서쌍에 대해 합한다. 같은 패턴을 두 번 고를 수 있고, 문자열이 같은 서로 다른 패턴 ID도 구분한다.',
      approach:'정방향 자동자로 각 위치 b에서 끝나는 패턴 수 end[b]를 구한다. 패턴과 본문을 뒤집은 자동자로 각 위치에서 시작하는 패턴 수 start를 구한다. 두 패턴 사이의 경계를 고정하면 왼쪽과 오른쪽을 독립적으로 고를 수 있다.',
      formula:String.raw`\mathrm{answer}=\sum_{b=0}^{N-2}\mathrm{end}[b]\cdot\mathrm{start}[b+1]`,
      reason:'이어 붙인 패턴의 한 등장은 두 패턴의 ID와 경계를 유일하게 정한다. 반대로 같은 경계에서 끝나는 패턴 하나와 시작하는 패턴 하나를 고르면 유효한 등장 하나가 된다.',
      hint:'terminal을 bool로 저장하면 중복 패턴을 놓친다. ID 개수를 저장하고 실패 링크를 따라 합산한 값을 상태별로 미리 구한다. 곱셈 전부터 64비트 정수를 사용한다.',
      complexity:String.raw`O(M+26V+N)`,costNote:'여기서 V는 두 자동자의 노드 수 합이다. 모든 패턴 쌍의 연결 문자열을 직접 만들지 않는다.',
    },
    {
      judge:'Codeforces',id:'963D',title:'Frequency of String',url:'https://codeforces.com/problemset/problem/963/D',
      stage:'확장',topic:'등장 위치와 슬라이딩 윈도',anchor:'output',concept:'출력 링크로 실제 매칭 위치 수집',
      task:'각 질의는 패턴 p와 정수 k를 준다. p를 적어도 k번 포함하는 본문의 가장 짧은 연속 부분 문자열 길이를 구한다. 불가능하면 −1이다.',
      approach:'자동자로 각 패턴의 등장 끝 위치를 오름차순으로 모은다. 끝 위치가 e₀,…,eᵣ₋₁이라면 연속한 k개를 덮는 구간 중 가장 짧은 것을 고른다.',
      formula:String.raw`\min_{0\le j\le r-k}\bigl(e_{j+k-1}-e_j+|p|\bigr)\quad(r\ge k)`,
      reason:'첫 등장의 시작부터 마지막 등장의 끝까지 남기면 k개의 등장을 포함하는 최소 구간이 된다. 어떤 최적 구간도 그 안의 연속한 k개 등장으로 줄일 수 있으므로 이 후보만 확인하면 된다.',
      hint:'등장 횟수만 누적해서는 위치 간 거리를 복원할 수 없다. 겹치는 등장도 저장한다. 패턴은 서로 다르며, 실제로 나열하는 매칭 수 Z에 비례하는 시간과 저장 공간이 필요하다.',
      complexity:String.raw`O(M+26V+N+Z+Q)`,costNote:'Q는 질의 수, Z는 모든 패턴의 실제 등장 횟수 합. 출력 링크를 따라 실제 terminal만 방문하는 구현 기준이다.',
    },
    {
      judge:'Codeforces',id:'163E',title:'e-Government',url:'https://codeforces.com/problemset/problem/163/E',
      stage:'도전',topic:'Failure Tree · Euler Tour · Fenwick',anchor:'count',concept:'실패 조상 조건을 서브트리 구간으로 바꾸기',
      task:'고정된 사전의 패턴을 활성화하거나 비활성화한다. 질의 본문마다 현재 활성 패턴들의 총 등장 횟수를 구한다. 초기에는 모든 패턴이 활성 상태이다.',
      approach:'실패 트리를 Euler Tour로 펴서 각 서브트리를 구간으로 만든다. 패턴 끝 노드 u를 켜면 u의 서브트리 전체에 +1, 끄면 −1을 더한다. 본문을 읽을 때 현재 상태의 점 값을 조회한다.',
      formula:String.raw`\mathrm{answer}(T)=\sum_{j=0}^{|T|-1}\mathrm{point}\bigl(\mathrm{tin}[\mathrm{state}_j]\bigr)`,
      reason:'u가 현재 상태 v의 실패 조상이라는 조건은 v가 u의 실패 트리 서브트리에 속한다는 조건과 같다. 따라서 구간 가산 후 v의 점 값은 지금 끝나는 활성 패턴의 수와 정확히 일치한다.',
      hint:'Fenwick Tree의 차분 배열로 구간 가산·점 질의를 처리한다. 이미 켜진 패턴을 다시 켜는 연산은 무시하도록 활성 여부를 별도로 저장한다. 각 본문은 루트에서 새로 탐색한다.',
      complexity:String.raw`O\bigl(M+26V+(K+U+H)\log(V+1)\bigr)`,costNote:'K는 패턴 수, U는 활성화·비활성화 연산 수, H는 질의 본문 길이 합. 초기 K개 패턴도 구간 가산으로 등록하는 구현 기준이다.',
    },
    {
      judge:'Codeforces',id:'696D',title:'Legen...',url:'https://codeforces.com/problemset/problem/696/D',
      stage:'도전',topic:'가중 자동자 · Max-plus 행렬',anchor:'transition',concept:'자동자 상태를 문자열 생성 DP의 상태로 사용',
      task:'패턴마다 양의 점수가 있다. 길이가 L 이하인 문자열을 만들어 모든 패턴의 등장 점수 합을 최대화한다. L은 최대 10¹⁴이다.',
      approach:'상태 v에 도착했을 때 얻는 점수 g[v]는 v와 실패 조상에서 끝나는 패턴 점수의 합이다. 문자 하나의 이동을 행렬 A에 저장하되, 불가능한 이동은 −∞로 둔다. 일반 곱셈 대신 Max-plus 곱셈으로 L번의 이동을 거듭제곱한다.',
      formula:String.raw`A_{uv}=\max_{c:\,\mathrm{go}(u,c)=v}g[v],\qquad (A\otimes B)_{ij}=\max_k(A_{ik}+B_{kj})`,
      reason:'자동자 상태가 같으면 이후 문자를 붙일 때 얻는 점수도 같다. 두 구간을 잇는 최댓값은 중간 상태 k별 최적 점수의 합 중 최댓값이다. 점수가 양수이므로 문자를 덧붙여 점수가 감소하지 않아 길이 L만 고려해도 된다.',
      hint:'같은 문자열의 패턴 점수도 모두 더한다. 루트에서 시작하는 벡터에 A의 L제곱을 적용하고 끝 상태 전체의 최댓값을 취한다. −∞에 대한 덧셈은 건너뛰고 점수는 64비트로 저장한다.',
      complexity:String.raw`O(M+26V+V^3\log L)`,costNote:'밀집 행렬 곱셈 기준. 패턴 길이 합이 작다는 조건을 이용하며, 일반적인 큰 사전에 그대로 적용할 수는 없다.',
    },
    {
      judge:'DOJ',id:'349',title:'쉬운 문자열 문제',url:'https://doj.kr/ko/problems/349',
      stage:'도전',topic:'자동자 DP → 행렬 거듭제곱',anchor:'transition',concept:'상태 전이와 terminal 보상의 결합',
      task:'주어진 패턴들이 총합으로 가장 많이 등장하도록 길이가 정확히 L인 문자열을 만든다. 겹치는 등장과 중복 패턴도 각각 센다. L은 최대 10⁹이다.',
      approach:'먼저 짧은 길이에 대해 dp[j][v]를 “j글자를 만들고 상태 v에 도착한 최대 등장 횟수”로 정의한다. w[v]는 실패 조상까지 포함한 terminal 개수이다. 아래 점화식을 한 단계 행렬로 바꾼 뒤 Max-plus 거듭제곱으로 확장한다.',
      formula:String.raw`\mathrm{dp}[j+1][\mathrm{go}(v,c)]\gets\max\bigl(\mathrm{dp}[j+1][\mathrm{go}(v,c)],\mathrm{dp}[j][v]+w[\mathrm{go}(v,c)]\bigr)`,
      reason:'새 문자를 붙여 추가되는 등장은 반드시 그 문자에서 끝난다. 그 수가 도착 상태의 w 값이다. 모든 문자열은 마지막 문자 전의 문자열과 그 문자로 유일하게 분해되므로 전이를 모두 고려하면 빠짐이 없다.',
      hint:'초기값은 dp[0][root]=0이고 나머지는 −∞이다. 길이별 DP는 이해와 작은 입력 검증에 사용하고, 전체 제약에서는 행렬 거듭제곱을 사용한다. 항등 행렬은 대각선 0, 나머지 −∞이다.',
      complexity:String.raw`O(M+26V+V^3\log L)`,costNote:'밀집 행렬 기준. 최대 501개 상태에서는 행렬 곱셈의 상수 비용도 크므로 자료 배치와 불가능한 전이 처리를 신경 쓴다. 답은 64비트 정수로 저장한다.',
    },
  ],
  suffix: [
    {
      judge:'AtCoder',id:'Practice2 I',title:'Number of Substrings',url:'https://atcoder.jp/contests/practice2/tasks/practice2_i',
      stage:'시작',topic:'서로 다른 부분 문자열',anchor:'applications',concept:'이전 접미사와 겹치는 접두사 빼기',
      task:'소문자 문자열 S에 나타나는 서로 다른 비어 있지 않은 부분 문자열의 개수를 구한다. 길이는 최대 500,000이다.',
      approach:'SA와 LCP를 구성한다. 각 접미사의 모든 접두사를 후보로 세고, 바로 이전 접미사와 이미 공유한 접두사 수를 뺀다.',
      formula:String.raw`\mathrm{answer}=\frac{N(N+1)}2-\sum_{r=1}^{N-1}\mathrm{LCP}[r]`,
      reason:'같은 접두사를 갖는 접미사는 SA에서 연속한다. 현재 접미사의 접두사가 이전에 등장했다면 바로 앞 접미사에도 등장하므로 LCP만큼 빼면 중복이 정확히 제거된다.',
      hint:'최대 답은 N(N+1)/2이므로 곱셈 전부터 64비트로 계산한다. Sentinel을 추가한 구현이라면 Sentinel 자체와 이를 포함한 부분 문자열을 답에 넣지 않는다.',
      complexity:String.raw`O(T_{\rm SA}(N)+N)`,costNote:'SA 구성 후 Kasai와 합산은 선형 시간이다.',
    },
    {
      judge:'DOJ',id:'602',title:'최장 공통 부분 문자열',url:'https://doj.kr/ko/problems/602',
      stage:'확장',topic:'일반화 Suffix Array',anchor:'generalized',concept:'서로 다른 원본에서 온 인접 접미사',
      task:'두 정수 수열 A, B에서 공통으로 나타나는 가장 긴 연속 부분 수열의 길이와 실제 원소를 출력한다. 제목과 달리 입력은 문자 문자열이 아니라 정수 수열이다.',
      approach:'두 수열의 값을 함께 좌표 압축하고 서로 다른 구분자를 끼워 A#B$를 만든다. SA에서 서로 다른 원본 소속의 유효 접미사가 인접한 경우의 LCP 최댓값을 찾는다.',
      formula:String.raw`K=\max_{\substack{1\le r<n\\\{\mathrm{owner}(\mathrm{SA}[r-1]),\mathrm{owner}(\mathrm{SA}[r])\}=\{A,B\}}}\mathrm{LCP}[r]`,
      reason:'공통 접두사를 갖는 접미사들의 SA 구간에 두 원본이 모두 있으면, 구간 안에서 원본 소속이 바뀌는 인접 쌍이 반드시 있다. 따라서 모든 쌍을 비교하지 않아도 최장 공통 구간을 찾을 수 있다.',
      hint:'구분자는 원래 값과 겹치지 않게 예약한다. 구분자에서 시작하는 접미사는 후보에서 제외하고, 답은 압축 번호가 아닌 원래 수열에서 복원한다. 연속하지 않아도 되는 LCS와는 다른 문제이다.',
      complexity:String.raw`O(T_{\rm SA}(n)+n\log n)`,costNote:'n=|A|+|B|+2. 좌표 압축 정렬을 포함한다. 이 문제는 DP로도 풀 수 있지만 여기서는 일반화 SA를 연습한다. 공통 원소가 없으면 K=0이다.',
    },
    {
      judge:'AtCoder',id:'ABC362 G',title:'Count Substring Query',url:'https://atcoder.jp/contests/abc362/tasks/abc362_g',
      stage:'확장',topic:'두 번의 이분 탐색',anchor:'search',concept:'패턴을 접두사로 갖는 SA 구간',
      task:'본문 S와 Q개의 패턴이 주어진다. 각 패턴의 등장 횟수를 구한다. 아호–코라식과 달리 본문을 먼저 색인하는 방식으로 풀어 본다.',
      approach:'본문의 SA를 한 번 구성한다. 각 패턴 P에 대해 P를 접두사로 갖는 접미사들의 반열린 구간 [L(P), R(P))를 이분 탐색한다.',
      formula:String.raw`\mathrm{occ}(P)=R(P)-L(P)`,
      reason:'P로 시작하는 모든 접미사는 사전순에서 한 구간을 이룬다. 각 접미사의 시작 위치가 곧 P의 한 등장 위치이므로 구간 크기가 등장 횟수이다.',
      hint:'비교할 때 P를 모두 소비하면 접미사가 더 길어도 “접두사 일치”로 처리한다. 접미사 전체와 P가 완전히 같은 경우만 세면 틀린다. 겹치는 등장도 서로 다른 시작 위치로 자연스럽게 포함된다.',
      complexity:String.raw`O\bigl(T_{\rm SA}(N)+M\log(N+1)+Q\bigr)`,costNote:'M은 모든 질의 패턴 길이 합. 매 비교에서 최대 |P|글자를 읽는 기본 이분 탐색 기준이며, 이 풀이에는 LCP나 RMQ가 필수는 아니다.',
      compare:{url:'/tutorial/aho-corasick/#exercise-atcoder-abc362-g',label:'같은 문제를 아호–코라식의 횟수 누적으로 풀기 →'},
    },
    {
      judge:'Codeforces',id:'427D',title:'Match & Catch',url:'https://codeforces.com/problemset/problem/427/D',
      stage:'도전',topic:'정확히 한 번 등장하는 공통 문자열',anchor:'generalized',concept:'LCP 구간의 양옆 경계',
      task:'두 문자열 각각에 정확히 한 번씩 등장하는 가장 짧은 공통 부분 문자열의 길이를 구한다. 없으면 −1을 출력한다.',
      approach:'고유한 구분자를 사용해 두 문자열을 합친다. 서로 다른 원본의 인접한 접미사 SA[r], SA[r+1]을 고른다. 둘은 일치하지만 바로 바깥 접미사들과는 일치하지 않는 가장 짧은 길이 d를 찾는다.',
      formula:String.raw`d_r=1+\max\bigl(\mathrm{LCP}[r],\mathrm{LCP}[r+2]\bigr),\qquad d_r\le\mathrm{LCP}[r+1]`,
      reason:'각 원본에 한 번씩 나타나는 문자열의 SA 구간에는 접미사가 정확히 두 개 있다. 두 접미사의 LCP 이하여야 공통이고, 양옆의 LCP보다 커야 세 번째 등장을 막는다. 이 두 조건이 필요충분하다.',
      hint:'배열 바깥의 LCP 값은 0으로 둔다. 구분자에서 시작한 접미사는 후보가 아니다. 가능한 d 중 최솟값을 구하며, 단순히 가장 큰 LCP를 찾는 문제와 구분한다.',
      complexity:String.raw`O(T_{\rm SA}(N)+N)`,costNote:'N은 구분자를 포함한 연결 문자열 길이. LCP의 앞 원소·뒤 원소 인덱스를 본문 정의와 맞춘다.',
    },
    {
      judge:'AtCoder',id:'ABC213 F',title:'Common Prefixes',url:'https://atcoder.jp/contests/abc213/tasks/abc213_f',
      stage:'도전',topic:'LCP 구간 최솟값 · 단조 스택',anchor:'rmq',concept:'모든 상대 접미사와의 LCP 합',
      task:'각 접미사에 대해 자기 자신을 포함한 모든 접미사와의 LCP 길이 합을 구한다. 문자열 길이는 최대 10⁶이다.',
      approach:'SA 순위 r에서 왼쪽과 오른쪽 상대를 나눈다. 각 방향으로 LCP 구간 최솟값의 합을 단조 스택으로 유지한다. 스택에 (최솟값, 그 값을 공유하는 구간 수)를 저장해 같은 기여를 묶는다.',
      formula:String.raw`\mathrm{ans}[\mathrm{SA}[r]]=N-\mathrm{SA}[r]+\sum_{l<r}\min_{l<t\le r}\mathrm{LCP}[t]+\sum_{r<q<N}\min_{r<t\le q}\mathrm{LCP}[t]`,
      reason:'두 접미사의 LCP는 두 순위 사이 LCP의 최솟값이다. 새 값을 추가하면 이전 구간들의 최솟값은 유지되거나 새 값으로 낮아진다. 스택에서 새 값 이상인 묶음을 합치면 각 구간의 기여를 빠짐없이 갱신할 수 있다.',
      hint:'자기 자신과의 LCP인 N−SA[r]을 따로 더한다. 양방향 순회 결과를 합치고 원래 시작 인덱스 순서로 출력한다. 모든 쌍을 RMQ로 질의하면 여전히 이차 시간이 든다. 합은 64비트로 저장한다.',
      complexity:String.raw`O(T_{\rm SA}(N)+N)`,costNote:'각 스택 원소는 한 번 들어가고 한 번 나온다. 큰 입력에서는 SA 구성 자체의 시간과 메모리도 확인한다.',
    },
    {
      judge:'AtCoder',id:'ABC272 F',title:'Two Strings',url:'https://atcoder.jp/contests/abc272/tasks/abc272_f',
      stage:'도전',topic:'순환 이동 · 같은 접두사 묶기',anchor:'search',concept:'길이가 고정된 구간의 사전순 비교',
      task:'길이 n인 문자열 S, T의 순환 이동을 각각 하나씩 골라 S 쪽이 사전순으로 T 쪽 이하가 되는 인덱스 쌍의 수를 구한다.',
      approach:'SS#TT$의 SA와 LCP를 구성한다. 인접 LCP가 n 이상인 접미사를 한 그룹으로 묶으면 앞 n글자가 같은 그룹이 된다. 각 원문의 첫 복사본에서 시작하는 n개 위치만 순환 이동 후보로 센다.',
      formula:String.raw`\mathrm{answer}=\sum_g t_g\left(s_g+\sum_{h<g}s_h\right)`,
      reason:'그룹은 길이 n인 문자열의 사전순으로 정렬되어 있다. T 후보 하나에 대해 앞선 그룹의 S 후보는 더 작고, 같은 그룹의 S 후보는 같다. 따라서 두 경우를 합하면 이하 조건을 정확히 센다.',
      hint:'s_g, t_g는 그룹 g의 유효한 S·T 후보 수이다. 후보가 아닌 접미사도 포함한 전체 SA에서 그룹을 만든 뒤 후보 수만 센다. 전체 접미사의 순위만 비교하면 앞 n글자가 같은 순환 이동을 잘못 구분한다. 주기적인 문자열도 위치가 다르면 별개의 선택이다.',
      complexity:String.raw`O(T_{\rm SA}(4n+2)+n)`,costNote:'구분자는 서로 다르고 원문 알파벳에 없어야 한다. 최대 n²개의 쌍을 세므로 답은 64비트 정수이다.',
    },
  ],
};
