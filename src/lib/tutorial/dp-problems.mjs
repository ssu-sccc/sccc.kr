// Learning stages are editorial guidance, not the judges' official ratings.
const at=(id,title,stage,topic,task,state,formula,hint,complexity)=>({judge:'AtCoder',id:`DP ${id.toUpperCase()}`,title,stage,topic,url:`https://atcoder.jp/contests/dp/tasks/dp_${id}`,task,state,formula,hint,complexity});
const cf=(id,title,stage,topic,task,state,formula,hint,complexity)=>({judge:'Codeforces',id,title,stage,topic,url:`https://codeforces.com/problemset/problem/${id.slice(0,-1)}/${id.slice(-1)}`,task,state,formula,hint,complexity});
const doj=(id,title,stage,topic,task,state,formula,hint,complexity)=>({judge:'DOJ',id:String(id),title,stage,topic,url:`https://doj.kr/ko/problems/${id}`,task,state,formula,hint,complexity});
export const dpProblems={
 basics:[
  at('a','Frog 1','시작','최소 비용','한두 칸씩 이동하는 개구리의 최소 이동 비용을 구한다.','dp[i]: i번째 돌에 도착하는 최소 비용. 이 강의는 0-index를 쓴다.',String.raw`dp[i]=\min_{j\in\{i-1,i-2\},\ j\ge0}(dp[j]+|h_i-h_j|)`,'출발 비용은 0. 마지막 점프를 지우면 선행 상태가 정해진다. 앞의 예제 풀이와 대응시킨다.',String.raw`O(N)`),
  at('b','Frog 2','시작','전이 확장','한 번에 최대 K칸 이동할 수 있도록 Frog 1을 확장한다.','dp[i]: i번째 돌까지의 최소 비용.',String.raw`dp[i]=\min_{\max(0,i-K)\le j<i}(dp[j]+|h_i-h_j|)`,'상태는 그대로 두고 들어오는 간선만 최대 K개로 늘린다. 최솟값을 고르는 범위와 실제 가능한 점프를 일치시킨다.',String.raw`O(NK)`),
  doj(599,'최대 부분 배열 문제','시작','끝점 상태','비어 있지 않은 연속 구간의 합 중 최댓값을 구한다.','dp[i]: 반드시 i에서 끝나는 비어 있지 않은 연속 구간의 최대 합.',String.raw`dp[0]=A_0,\qquad dp[i]=\max(A_i,dp[i-1]+A_i)`,'현재 원소에서 새로 시작하거나 이전 구간을 연장한다. 답은 모든 dp[i]의 최댓값이다. 모두 음수인 입력도 있으므로 빈 구간의 합 0을 정답 후보로 넣지 않는다. 합은 64비트 정수로 저장한다.',String.raw`O(N)`),
  at('h','Grid 1','시작','격자 경로','막힌 칸을 피하며 오른쪽·아래로 이동하는 경로 수를 구한다.','dp[r][c]: 해당 칸까지의 경로 수.',String.raw`dp[r][c]=\begin{cases}0&\text{막힌 칸},\\dp[r-1][c]+dp[r][c-1]&\text{그 외}.\end{cases}`,'시작점은 1, 범위 밖은 0으로 별도 처리한다. 원문의 나머지 조건을 적용한다. 장애물은 정점을 제거한 것과 같다.',String.raw`O(HW)`),
  at('c','Vacation','확장','마지막 선택','같은 활동을 연속으로 하지 않으면서 행복도 합을 최대화한다.','dp[i][a]: i일까지 활동하고 마지막 활동이 a일 때의 최대 행복도.',String.raw`dp[i][a]=p_{i,a}+\max_{b\ne a}dp[i-1][b]`,'날짜만 기억하면 내일 할 수 없는 활동을 알 수 없다. 마지막 활동을 상태에 추가하고 첫날은 각 활동의 점수로 초기화한다.',String.raw`O(N\cdot3^2)=O(N)`),
  cf('698A','Vacations','확장','마지막 선택','날마다 가능한 활동이 다를 때 최소 휴식 일수를 구한다.','dp[i][a]: i일을 마쳤고 마지막 행동이 휴식·대회·운동 중 a인 최소 휴식 수.',String.raw`dp[i][a]=[a=\mathrm{rest}]+\min_{b\ \mathrm{compatible}\ a}dp[i-1][b]`,'휴식은 항상 허용되고 연속 휴식도 가능하다. 나머지는 그날 허용된 활동만 쓰고 전날과 같은 활동을 금지한다.',String.raw`O(N)`),
  cf('455A','Boredom','확장','값 기준 압축','선택한 값과 이웃 값의 삭제 규칙 아래 점수 합을 최대화한다.','best[x]: 값 1..x만 고려한 최대 점수. cnt[x]는 등장 횟수.',String.raw`best[x]=\max(best[x-1],best[x-2]+x\cdot cnt[x])`,'양의 같은 값들은 함께 고르는 것이 이득이다. 입력 인덱스가 아니라 값의 축에 상태를 만들고 x−1과 x의 동시 선택을 막는다. x=1은 따로 처리한다.',String.raw`O(N+\max a_i)`),
  cf('189A','Cut Ribbon','확장','정확히 채우기','길이 n을 세 종류의 길이로 정확히 나누어 조각 수를 최대화한다.','dp[x]: 정확히 길이 x를 만든 최대 조각 수. dp[0]=0, 불가능은 −∞.',String.raw`dp[x]=\max_{\ell\in\{a,b,c\},\ \ell\le x}(dp[x-\ell]+1)`,'남는 길이를 허용하지 않는다. 초기값을 모두 0으로 두면 불가능한 길이도 가능한 것처럼 이어 붙이게 된다.',String.raw`O(n)`),
  at('d','Knapsack 1','확장','선택과 자원','무게 제한 안에서 물건을 한 번씩 골라 가치 합을 최대화한다.','dp[i][w]: 앞 i개 물건만 써서 무게 한도 w 이하로 얻는 최대 가치.',String.raw`dp[i][w]=\max(dp[i-1][w],dp[i-1][w-w_i]+v_i)`,'두 번째 후보는 w≥w_i일 때만 사용한다. 1차원 압축에서는 무게를 감소시키며 순회해 같은 물건을 재사용하지 않는다.',String.raw`O(NW)`),
  doj(601,'최장 증가 부분 수열','도전','LIS 최적화 · 복원','엄격히 증가하는 최장 부분 수열의 길이와 실제 원소들을 출력한다.','dp[i]: A[i]로 끝나는 LIS 길이. parent[i]에는 선택한 이전 인덱스를 남긴다.',String.raw`dp[i]=1+\max\bigl(\{0\}\cup\{dp[j]:j<i,\ A_j<A_i\}\bigr)`,'O(N²)는 N≤5000 서브태스크용이다. 전체 N≤500000에서는 값 좌표를 압축하고 Fenwick Tree에 (최대 길이, 끝 인덱스)를 저장한다. 현재 값보다 작은 좌표의 최댓값을 질의해 parent를 기록한 뒤 현재 좌표를 갱신한다. 최종 끝점에서 역추적해 원소를 뒤집는다.',String.raw`O(N\log N)\text{ 시간},\ O(N)\text{ 공간}`),
  doj(600,'최장 공통 부분 수열','복원','정수 수열 LCS','두 정수 수열의 LCS 길이와 실제 공통 부분 수열 하나를 출력한다.','dp[i][j]: 두 수열의 길이 i,j 접두사 사이 LCS 길이.',String.raw`dp[i][j]=\max(dp[i-1][j],dp[i][j-1],\ dp[i-1][j-1]+1\ \text{if }A_{i-1}=B_{j-1})`,'위·왼쪽 이동은 원소를 버리고, 같은 원소를 선택한 대각 이동에서만 값을 기록한다. 문자열이 아니라 정수 벡터를 복원한다. 원문은 여러 테스트 케이스이며 길이가 0이면 원소를 출력하지 않는다.',String.raw`O(NM)`),
  at('f','LCS','복원','2차원 역추적','두 문자열의 최장 공통 부분 수열 하나를 출력한다.','dp[i][j]: 두 문자열의 길이 i,j 접두사 사이 LCS 길이.',String.raw`dp[i][j]=\max(dp[i-1][j],dp[i][j-1],\ dp[i-1][j-1]+1\ \text{if }s_{i-1}=t_{j-1})`,'같은 문자를 선택한 대각 이동만 복원 문자열에 넣는다. 위·왼쪽 이동은 문자를 버린다. 뒤에서 모은 문자는 마지막에 뒤집는다.',String.raw`O(|s||t|)`),
 ],
 bit:[
  at('o','Matching','시작','집합만 남기기','허용된 쌍만 사용해 두 집합 사이 완전 매칭의 수를 센다.','dp[S]: 앞 |S|명을 S에 속한 상대들과 연결한 경우의 수.',String.raw`dp[S\cup\{j\}]\mathrel{+}=dp[S]\quad(j\notin S,\ a_{|S|,j}=1)`,'현재 처리할 사람은 popcount(S)로 복원된다. TSP와 달리 마지막 상대를 추가 축으로 남길 필요가 없다. dp[∅]=1.',String.raw`O(N2^N)`),
  doj(603,'외판원 순회 문제','복원','TSP · 경로 복원','모든 정점을 한 번 방문하고 시작점으로 돌아오는 최소 비용과 방문 순서를 구한다.','dp[S][v]: 시작점 0에서 정확히 S를 방문하고 v에서 끝나는 최소 비용.',String.raw`dp[S\cup\{u\}][u]\gets\min(dp[S\cup\{u\}][u],dp[S][v]+W_{v,u})`,'원문에서 −1은 없는 간선이며 0은 유효한 비용이다. 전이와 귀환 간선의 존재를 검사한다. 갱신할 때 이전 끝점을 기록하고, 귀환 비용까지 최소인 끝점에서 복원한다. 순환이 없으면 −1, 있으면 비용과 1-index 정점 순열을 출력한다. 합은 64비트로 계산한다.',String.raw`O(N^2 2^N)`),
  {judge:'AtCoder',id:'ABC180 E',title:'Traveling Salesman among Aerial Cities',stage:'확장',topic:'비대칭 TSP',url:'https://atcoder.jp/contests/abc180/tasks/abc180_e',task:'좌표로 주어진 비대칭 이동 비용으로 모든 도시를 순회한다.',state:'좌표는 먼저 간선 비용으로 바꾸고, TSP의 집합·끝점 상태를 사용한다.',formula:String.raw`w(i,j)=|x_i-x_j|+|y_i-y_j|+\max(0,z_j-z_i)`,hint:'올라가는 비용과 내려가는 비용이 다르다. 비용 행렬을 대칭으로 채우면 안 된다. 마지막에는 출발점으로 돌아온다.',complexity:String.raw`O(N^2 2^N)`},
  cf('580D','Kefa and Dishes','확장','집합 + 마지막 선택','서로 다른 요리 m개를 순서대로 먹어 기본 만족도와 연속 보너스 합을 최대화한다.','dp[S][v]: S의 요리를 먹고 v로 끝나는 최대 만족도.',String.raw`dp[S\cup\{u\}][u]\gets\max(dp[S\cup\{u\}][u],dp[S][v]+a_u+b_{v,u})`,'모든 단일 요리에서 시작한다. 답은 |S|=m인 상태의 최댓값이며 시작점으로 돌아가지 않는다. 보너스는 바로 직전 요리와의 관계만 사용한다.',String.raw`O(N^2 2^N)`),
  {judge:'AtCoder',id:'ABC196 D',title:'Hanjo',stage:'확장',topic:'Profile · 점유와 개수',url:'https://atcoder.jp/contests/abc196/tasks/abc196_d',task:'직사각형을 도미노 A개와 1×1 타일 B개로 덮는 방법을 센다.',state:'dp[p][mask][a]: 앞 p칸을 처리했고, 이후 W칸의 점유가 mask이며 사용한 도미노 수가 a인 경우의 수.',formula:String.raw`\mathrm{answer}=dp[HW][0][A]`,hint:'점유된 칸은 넘긴다. 빈 칸에서는 1×1 또는 가로·세로 도미노를 놓는다. 도미노를 놓을 때만 a를 늘리고 a≤A를 유지한다. 행 경계·범위·겹침을 검사한다. 2A+B=HW이므로 완성 시 도미노 수가 A이면 1×1 타일 수도 B이다. 나머지 연산 없이 센다.',complexity:String.raw`O(HW(A+1)2^W)`},
  cf('991D','Bishwock','확장','Profile · 두 칸 경계','막힌 칸이 있는 2×N 보드에 겹치지 않는 L자 타일을 최대한 배치한다.','dp[c][mask]: c열 직전까지 처리하고 왼쪽 타일이 c열에서 차지한 두 칸이 mask일 때의 최대 배치 수.',String.raw`dp[c+1][next]\gets\max(dp[c+1][next],dp[c][mask]+k)`,'빈칸을 남겨도 된다. 새 타일 없이 넘기는 경우(k=0)와 c,c+1 두 열에 걸친 L자 네 방향을 놓는 경우(k=1)를 검사한다. 같은 두 열에서 시작하는 L자 두 개는 반드시 겹친다. 기존 점유·장애물·마지막 열을 검사하고 다음 열 점유만 넘긴다. 상태와 전이 수가 열마다 상수이다.',String.raw`O(N)`),
  at('u','Grouping','도전','부분집합 분할','원소들을 여러 그룹으로 나누어 같은 그룹 내부의 쌍 점수 합을 최대화한다.','dp[S]: S를 여러 그룹으로 분할한 최대 점수. score[T]는 그룹 T 하나의 점수.',String.raw`dp[S]=\max_{\varnothing\ne T\subseteq S}(dp[S\setminus T]+score[T])`,'dp[∅]=0. 중복된 분할 순서를 줄이려면 S의 최소 원소를 포함한 T만 선택한다. 여기서는 원소 하나가 아니라 부분집합 전체를 고른다.',String.raw`O(3^N+N^2 2^N)`),
 ],
 tree:[
  at('p','Independent Set','시작','선택 DP · 개수','인접한 두 정점을 동시에 검게 칠하지 않는 흰색·검은색 배치 수를 센다.','white[v], black[v]: v의 색을 고정한 부분트리 배치 수.',String.raw`white[v]=\prod_c(white[c]+black[c]),\quad black[v]=\prod_c white[c]`,'최대 가치를 구한 본문의 max/합을 그대로 쓰면 안 된다. 한 자식의 대안은 더하고, 서로 독립인 자식 선택은 곱한다. 리프의 두 값은 각각 1.',String.raw`O(N)`),
  doj(346,'트리 뽑아내기','확장','연결 부분트리','리프를 반복해서 제거한 뒤 남는 정점들의 가중치 합을 최대화한다.','down[v]: v의 부분트리 안에서 v를 포함하는 비어 있지 않은 연결 집합의 최대 합.',String.raw`down[v]=A_v+\sum_c\max(0,down[c]),\qquad \mathrm{answer}=\max_v down[v]`,'리프 삭제는 연결성을 유지한다. 반대로 남길 연결 집합을 정하면 그 바깥을 먼 리프부터 제거할 수 있다. 자식 방향의 음수 기여는 버린다. 최적 집합의 가장 높은 정점을 v로 잡으면 down[v]에 포함되므로 모든 v의 최댓값을 취한다. 전부 음수여도 정점 하나는 남겨야 하며 합은 64비트로 계산한다.',String.raw`O(N)`),
  cf('1528A',"Parsa's Humongous Tree",'확장','두 경계값 상태','정점마다 구간 안의 값을 골라 모든 간선의 양 끝 값 차이의 절댓값 합을 최대화한다.','x[v][0]=l_v, x[v][1]=r_v. dp[v][s]는 v의 값을 x[v][s]로 고정한 부분트리의 최대 합.',String.raw`dp[v][s]=\sum_c\max_{t\in\{0,1\}}\bigl(dp[c][t]+|x[v][s]-x[c][t]|\bigr)`,'이웃 값을 고정하면 한 정점 값에 대한 목적함수는 절댓값들의 합인 볼록 함수라 구간 끝점 중 하나에서 최대가 된다. 최적해의 각 좌표를 차례로 끝점으로 옮겨도 값이 줄지 않으므로 두 상태면 충분하다. 부모 값을 고정한 뒤 자식들을 독립적으로 합친다.',String.raw`O(N)`),
  cf('1646D','Weight the Tree','복원','우선순위 최적화 · 독립 집합','이웃 가중치 합과 자기 가중치가 같은 정점 수를 최대화하고, 그중 전체 가중치 합을 최소화한다.','N>2에서 good 정점을 선택하는 독립 집합 DP. D[v][s]=(good 수, 가중치 합)를 저장하고 첫 값 최대·둘째 값 최소로 비교한다.',String.raw`D[v][1]=(1,\deg v)+\sum_cD[c][0],\quad D[v][0]=(0,1)+\sum_c\operatorname{best}(D[c][0],D[c][1])`,'양의 가중치에서 인접 정점 둘이 모두 good이면 둘 다 다른 이웃이 없어야 하므로 N>2에서는 불가능하다. 선택 정점에는 차수, 나머지에는 1을 주면 선택 정점이 good이 된다. 쌍의 합은 성분별로 계산하고 비교는 good 수 우선이다. 선택을 복원해 가중치를 출력한다. N=2는 (1,1)로 두 정점 모두 good인 예외이다.',String.raw`O(N)`),
  cf('1092F','Tree with Maximum Cost','확장','가중 거리 Rerooting','루트에서 각 정점까지 거리×정점 가중치의 합을 최대화한다.','F[v]: v 기준 전체 가중 거리 합. sub[c]는 자식 c의 부분트리 가중치 합.',String.raw`F[c]=F[v]+W-2\,sub[c]`,'c 쪽 정점은 거리 1이 줄고 나머지는 1이 늘어난다. W는 전체 가중치 합이다. 모든 정점의 F를 계산한 뒤 최댓값을 출력한다.',String.raw`O(N)`),
  cf('1324F','Maximum White Subtree','도전','모든 방향의 최댓값','각 정점을 포함하는 연결 부분트리에서 흰 정점 수−검은 정점 수를 최대화한다.','흰색은 +1, 검은색은 −1. 방향 메시지는 그 성분에서 얻을 수 있는 연결 기여이다.',String.raw`m_{v\to u}=w_v+\sum_{x\in N(v)\setminus\{u\}}\max(0,m_{x\to v})`,'나쁜 방향은 연결하지 않아도 되어 0과 비교한다. 다만 현재 정점 자체는 반드시 포함한다. 전체 답은 자신과 모든 이웃 방향의 유리한 기여를 합친다.',String.raw`O(N)`),
  at('v','Subtree','도전','곱셈 Rerooting','각 정점을 검게 고정하고 검은 정점들이 하나의 연결 집합을 이루는 색칠 수를 센다.','m[v→u]: u 방향을 제외한 성분에서 v를 포함하는 연결 집합의 수.',String.raw`m_{v\to u}=\prod_{x\in N(v)\setminus\{u\}}(1+m_{x\to v})\pmod M`,'각 이웃 방향은 선택하지 않거나 이웃을 포함해 연결한다. 법 M은 소수라는 보장이 없다. total/child 대신 prefix/suffix 곱으로 제외한다.',String.raw`O(N)`),
 ],
};
